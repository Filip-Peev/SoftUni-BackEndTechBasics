function myFunction(firstNumber, secondNumber, thirdNumber) {
    'use strict';

    const result = Math.max(firstNumber, secondNumber, thirdNumber);
    console.log(`The largest number is ${result}.`);
}